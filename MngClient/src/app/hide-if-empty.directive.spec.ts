import { HideIfEmptyDirective } from './hide-if-empty.directive';

describe('HideIfEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new HideIfEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
